# Tanks Wars

## Текущая версия

`1.1.1.1v34`

Правило версий: если базовая версия не меняется, следующая правка получает следующий суффикс: `1.1.0.3v2`, затем `1.1.0.3v3` и так далее.

После каждой правки нужно писать, какие файлы были изменены. Все дальнейшие изменения кода, стилей, данных и документации нужно заносить в этот README отдельным блоком версии.

## Изменения `1.1.1.1v34`

- Ветка Sheridan выровнена по требуемым уровням: `M24 Chaffee` VI, `M41 Walker Bulldog` VII, `T49` VIII, `T92E1` IX, `M551 Sheridan (ПТУР)` X.
- Добавлены соединительные IV–V уровни перед Chaffee и удалён лишний промежуточный Sheridan, чтобы линия не имела скачков.
- В остальных ветках США добавлены недостающие соединительные узлы VII–IX уровней перед `T110E3`, `T110E4`, `T110E5` и `T92 HMC`; продолжений после X нет.

Затронутые файлы:

- `data.csv`
- `README.md`
- `tools/add-usa-tech-tree.mjs`
- `tools/set-usa-tier-x.mjs`
- `BALANCE_REPORT.md`
- `data.md`

Проверки: оба инструмента прошли `node --check`, ключевые уровни проверены по `data.csv`, отчёты пересозданы после изменения данных, `git diff --check` прошёл.

## Изменения `1.1.1.1v33`

- Финальными X уровнями американских веток назначены `M551 Sheridan (ПТУР)`, `T110E3`, `T110E4`, `T110E5`, `M48 Patton`, `M-V-Y`, `M67A2` и `T92 HMC`.
- Удалены продолжения после этих машин и добавлены соединительные VIII–IX уровни, поэтому внутри линий снова нет пропусков и уровней выше X.
- Линия Sheridan сохранена с ПТУР на финальном танке; огнемётная ветка заканчивается на `M67A2` X уровня.

Затронутые файлы:

- `data.csv`
- `README.md`
- `tools/add-usa-tech-tree.mjs`
- `tools/set-usa-tier-x.mjs`
- `BALANCE_REPORT.md`
- `data.md`

Проверки: оба инструмента прошли `node --check`, отчёты пересозданы после изменения `data.csv`, форматирование `git diff --check` прошло.

## Изменения `1.1.1.1v32`

- Исправлены лишние узлы после X уровня в американском дереве: удалены ошибочные XI уровни `AGS XM8A1`, `M1 Abrams`, `T110E3B`, `T110E5C` и `T110E4C`.
- Генератор дерева теперь формирует линии строго в диапазоне I–X и больше не добавляет продолжения после десятого уровня.

Затронутые файлы:

- `data.csv`
- `README.md`
- `tools/add-usa-tech-tree.mjs`
- `BALANCE_REPORT.md`
- `data.md`

Проверки: синтаксис генератора проверен, отчёты пересозданы; поиск узлов XI уровня в `data.csv` ничего не нашёл.

## Изменения `1.1.1.1v31`

- Сформировано непрерывное дерево США с единым корнем I уровня `M2 Light`.
- От корня без пропусков уровней расходятся ЛТ-ветка `M3 Stuart → ... → M551 Sheridan → M551 Sheridan (ПТУР)`, СТ-ветка с `M48 Patton`, ПТ-ветка `T110E3`, три тяжёлые линии `T110E5`, `T110E4` и Yoh, САУ со II уровня, а также огнемётная ветка.
- Все добавленные машины отмечены как будущие (`AC=0`), поэтому характеристики оставлены пустыми.
- Для каждой линии заданы связи следующего уровня; независимые обрывки с V–VIII уровня больше не используются.

Затронутые файлы:

- `data.csv`
- `README.md`
- `tools/add-usa-tech-tree.mjs`
- `BALANCE_REPORT.md`
- `data.md`

Проверки: `node --check tools/add-usa-tech-tree.mjs`, генерация отчётов баланса и справочника после изменения `data.csv`.

## Изменения `1.1.1.1v30`

- Артиллерийский снаряд теперь игнорирует танки и препятствия на промежуточном участке траектории.
- Проверка попадания для САУ выполняется только в конечной точке после разброса, поэтому препятствие или танк на линии выстрела не останавливает снаряд раньше времени.

Затронутые файлы:

- `README.md`
- `TESTING.md`
- `index.html`
- `js/06-battle-logic.js`

Проверки:

- `node --check js/06-battle-logic.js` — успешно.
- `git diff --check` — успешно; остаются только информационные предупреждения Git о преобразовании документации LF в CRLF.
- `data.csv` не изменялся, баланс-отчёты не пересоздавались.

## Изменения `1.1.1.1v29`

- Исправлена дальность артиллерийского снаряда: после применения поперечного `±1×` и продольного `±3×` разброса он теперь завершается в рассчитанной точке падения и не продолжает полёт бесконечно.
- Если по пути нет танка или препятствия, снаряд фиксирует промах именно на границе своей рассчитанной дальности.

Затронутые файлы:

- `README.md`
- `TESTING.md`
- `index.html`
- `js/06-battle-logic.js`

Проверки:

- `node --check js/06-battle-logic.js` — успешно.
- `git diff --check` — успешно; остаются только информационные предупреждения Git о преобразовании документации LF в CRLF.
- `data.csv` не изменялся, баланс-отчёты не пересоздавались.

## Изменения `1.1.1.1v28`

- Снаряд САУ игрока теперь строит траекторию от фактической точки клика прицела, а не только от текущего угла ствола.
- Разброс САУ разделён по двум осям относительно направления на точку прицела: поперёк направления используется случайное смещение `±1×разброс`, вдоль направления — `±3×разброс`.
- Для САУ-ботов применяется та же модель относительно координат выбранной цели; обычные танки сохраняют прежний угловой разброс.
- В журнале выстрела для артиллерии сохраняется фактическая целевая точка с учётом разброса.

Затронутые файлы:

- `README.md`
- `TESTING.md`
- `index.html`
- `js/06-battle-logic.js`

Проверки:

- `node --check js/06-battle-logic.js` — успешно.
- `git diff --check` — успешно; остаются только информационные предупреждения Git о преобразовании документации LF в CRLF.
- `data.csv` не изменялся, баланс-отчёты не пересоздавались.

## Изменения `1.1.1.1v27`

Проведён отдельный ребаланс активных САУ Германии и СССР. В каждой строке ниже указаны только изменённые характеристики в формате «было → стало». Урон указан сразу для всех трёх одинаковых ОФ-снарядов (`E/F/G`), задержка движения — поле `S`.

Германия:

| Машина | HP (`P`) | Урон 1/2/3 (`E/F/G`) | Перезарядка (`Q`) | Задержка движения (`S`) |
|---|---:|---:|---:|---:|
| G.Pz. Mk. VI (e) | 120 → 280 | 80/80/80 → 100/100/100 | 8 → 5,6 с | 0,082 → 0,04 |
| Sturmpanzer I bison | 180 → 350 | 160/160/160 → 170/170/170 | 15 → 7,2 с | 0,082 → 0,04 |
| Pz.Sfl. Ivb | 240 → 580 | 240/240/240 → 260/260/260 | 16 → 8 с | 0,082 → 0,042 |
| Grile | 300 → 760 | 320/320/320 → 300/300/300 | 16 → 10 с | 0,082 → 0,045 |
| Hummel | 360 → 950 | — | 20 → 12,8 с | 0,082 → 0,045 |
| G.W. Panther | 420 → 1150 | 480/480/480 → 500/500/500 | 20 → 14,5 с | 0,082 → 0,045 |
| G.W. Tiger (P) | 480 → 1300 | 560/560/560 → 600/600/600 | 20 → 15,7 с | 0,082 → 0,05 |
| G.W. Tiger | 540 → 1550 | 640/640/640 → 700/700/700 | 20 → 17,7 с | 0,082 → 0,05 |
| G.W. E100 | 600 → 1950 | 720/720/720 → 900/900/900 | 18 → 19,5 с | 0,082 → 0,055 |

СССР:

| Машина | HP (`P`) | Урон 1/2/3 (`E/F/G`) | Перезарядка (`Q`) | Задержка движения (`S`) |
|---|---:|---:|---:|---:|
| СУ-18 | 280 → 260 | — | 4 → 4,2 с | 0,0257 → 0,04 |
| СУ-26 | 330 → 340 | 70/70/70 → 80/80/80 | 3 → 3,4 с | 0,0257 → 0,04 |
| СУ-5 | 350 → 600 | 200/200/200 → 190/190/190 | 6 → 5,8 с | 0,0257 → 0,042 |
| СУ-122А | 670 → 800 | 340/340/340 → 300/300/300 | 8 → 10 с | 0,0257 → 0,042 |
| СУ-8 | 730 → 950 | 340/340/340 → 400/400/400 | 7 → 12,8 с | 0,0257 → 0,045 |
| СУ-14-1 | 2170 → 1200 | 620/620/620 → 500/500/500 | 10 → 14,5 с | 0,0257 → 0,045 |
| СУ-14-2 | 340 → 1350 | 620/620/620 → 650/650/650 | 9 → 15 с | 0,0257 → 0,05 |
| 212А | 2330 → 1550 | 690/690/690 → 700/700/700 | 10 → 17,7 с | 0,0257 → 0,05 |
| объект 261 | 2500 → 1950 | 960/960/960 → 900/900/900 | 10 → 19,5 с | 0,0257 → 0,055 |

Пробитие, броня, типы снарядов, разброс и остальные поля у этих машин не менялись. Пустые будущие ветки САУ без боевых характеристик также не изменялись.

Затронутые файлы:

- `README.md`
- `data.csv`
- `BALANCE_REPORT.md`
- `data.md`

Проверки:

- `node tools/generate-balance-report.mjs` — успешно, создан отчёт на 447 танков.
- `node tools/generate-data-md.mjs` — успешно, создана документация на 447 танков.
- Ручная проверка строк САУ в `BALANCE_REPORT.md`: все 18 изменённых активных машин находятся в диапазоне `В балансе`; немецкие выбросы `В ГОВНЕ` устранены, советские высокоуровневые выбросы `Сильный` устранены.
- `data.csv` изменён, поэтому `BALANCE_REPORT.md` и `data.md` пересозданы.

## Изменения `1.1.1.1v26`

- Скорость поворота башни ботов теперь использует ту же классовую настройку, что и игрок: ЛТ, СТ, ТТ, КТ и БТР больше не получают отдельный ускоренный минимум.

Затронутые файлы:

- `README.md`
- `TESTING.md`
- `index.html`
- `js/06-battle-logic.js`

Проверки:

- `node --check js/06-battle-logic.js` — успешно.
- `git diff --check` — успешно; остаются только информационные предупреждения Git о преобразовании документации LF в CRLF.
- `data.csv` не изменялся, поэтому баланс-отчёт не пересоздавался.

## Изменения `1.1.1.1v25`

- Знаки классности теперь назначаются по результату конкретного боя: учитываются нанесённый и заблокированный урон, помощь, уничтожения, захват базы и победа.
- Для каждого танка сохраняется максимальный достигнутый знак; накопленные бои и средние показатели больше не создают знак сами по себе.
- Максимальный знак отображается рядом с танком в ангаре и в списке техники профиля.

Затронутые файлы:

- `README.md`
- `TESTING.md`
- `index.html`
- `js/02-hangar-tech-tree.js`
- `js/06-battle-logic.js`

Проверки:

- `node --check js/02-hangar-tech-tree.js` — успешно.
- `node --check js/06-battle-logic.js` — успешно.
- `git diff --check` — успешно; Git сообщает только об ожидаемом преобразовании LF в CRLF для документации.
- `data.csv` не изменялся в рамках этой правки, поэтому баланс-отчёт не пересоздавался.

## Изменения `1.1.1.1v24`

- По запросу удалены звуковые эффекты, фоновая музыка и голосовая озвучка команд.
- Из настроек удалены громкость эффектов, громкость музыки и переключатель озвучки; команды, разрушения карты и знаки мастерства продолжают работать визуально.
- Удалены вызовы воспроизведения звука из боевых событий, чтобы браузер не создавал `AudioContext` и не использовал `speechSynthesis`.
- Версии подключаемых файлов обновлены до `1.1.1.1v24`.

Затронутые файлы:

- `README.md`
- `TESTING.md`
- `index.html`
- `js/00-state.js`
- `js/04-ui-developer.js`
- `js/06-battle-logic.js`
- `js/08-startup.js`

Проверки:

- `node --check` для изменённых JavaScript-файлов — успешно.
- `git diff --check` — успешно; остаются только информационные предупреждения Git о преобразовании документации из LF в CRLF.
- `data.csv` не изменялся, баланс-отчёты не пересоздавались.

## Изменения `1.1.1.1v23`

- Добавлен процедурный звуковой движок Web Audio: пространственно затухающие выстрелы, попадания, пробития, рикошеты, взрывы, обрушения и сигналы результата боя.
- Добавлена фоновая боевая музыка; в настройках появились отдельная громкость эффектов, громкость музыки и переключатель голосовых команд.
- Реализована русская озвучка приказов через системный синтез речи браузера.
- По клавише `C`/`С` и мобильной кнопке `Ком` открывается меню работающих приказов: атака выбранной у курсора цели, следование, удержание позиции, защита базы и отступление.
- Приказы изменяют цели и маршруты союзных ботов, имеют 18-секундный срок действия и отображаются золотым маркером непосредственно на карте.
- Союзные боты получили командную инициативу: назначенный наиболее боеспособный бот оценивает захват базы, соотношение сил и опасные цели, после чего сам отдаёт озвученный приказ всей команде.
- Добавлена новая крупная карта `Осада сталелитейного` размером `4200×2800`: производственные корпуса, две дорожные магистрали, река, воронки, пожары, дым, остовы, растительность и 17 разрушаемых объектов.
- Стены, склады и топливные ёмкости имеют прочность и до разрушения блокируют движение, обзор и снаряды. После уничтожения они освобождают проход и оставляют динамические обломки, дым и воронку.
- Топливные ёмкости взрываются с уроном по площади и оставляют очаг пожара; число разрушенных игроком объектов добавлено в послебоевую статистику.
- Добавлены видимые знаки мастерства техники: III, II, I степени и `Мастер`. Степень рассчитывается по боям, среднему урону, победам и уничтожениям и показывается в карточках ангара и списке техники профиля.
- Обучение и тестовая документация дополнены управлением приказами, проверками звука, разрушений, поведения ботов и знаков мастерства.
- Версии всех изменённых подключаемых файлов обновлены до `1.1.1.1v23` для сброса браузерного кеша.

Затронутые файлы:

- `README.md`
- `TESTING.md`
- `index.html`
- `css/styles.css`
- `js/00-state.js`
- `js/02-hangar-tech-tree.js`
- `js/03-events-store-profile.js`
- `js/04-ui-developer.js`
- `js/05-battle-maps.js`
- `js/06-battle-logic.js`
- `js/07-battle-render.js`
- `js/08-startup.js`

Проверки:

- `node --check` для всех девяти игровых JavaScript-файлов — успешно, синтаксических ошибок нет.
- `git diff --check` — успешно; найдены только информационные предупреждения Git о будущем преобразовании `README.md` и `TESTING.md` из LF в CRLF.
- Попытка проверки в headless Chromium через Playwright не выполнена: пакет доступен, но браузерный бинарник Playwright в окружении не установлен.
- `data.csv` не изменялся, поэтому `BALANCE_REPORT.md` и `data.md` пересоздавать не требуется.

## Изменения `1.1.1.1v22`

- Отменены искусственные преимущества ботов из `1.1.1.1v21`: возвращены обычные время реакции, скорость поворота башни, разброс орудия и требования к наведению.
- Умное принятие решений сохранено: боты по-прежнему оценивают броню цели, остаток боекомплекта, возможность добивания и угрозу на ближней дистанции.
- Координация огня улучшена без усиления характеристик: бот поддерживает союзный огонь, пока цели недостаточно назначенного урона, но получает большой штраф выбора цели при ожидаемом избыточном уроне. Это распределяет выстрелы команды между противниками и уменьшает бессмысленный фокус уже обречённой машины.
- Версия загрузки боевой логики обновлена для сброса браузерного кеша.

Затронутые файлы:

- `README.md`
- `index.html`
- `js/06-battle-logic.js`

Проверки:

- `node --check js/06-battle-logic.js` — успешно, синтаксических ошибок нет.
- `git diff --check` — успешно; найдено только информационное предупреждение Git о будущем преобразовании `README.md` из LF в CRLF.
- `data.csv` не изменялся, поэтому баланс-отчёты пересоздавать не требуется.

## Изменения `1.1.1.1v21`

- Боевые боты существенно усилены: время принятия решений сокращено, а башни быстрее доворачиваются к выбранной цели.
- Боты теперь выбирают из оставшегося боекомплекта наиболее эффективный против брони цели снаряд и повышают приоритет ОФ при добивании тяжело повреждённых машин.
- Выбор цели учитывает возможность уничтожения одним выстрелом, угрозу на ближней дистанции и уже выбранную союзными ботами цель, благодаря чему команды концентрируют огонь.
- Точность ботов повышена: они дожидаются более точного сведения, а их случайный разброс уменьшен до 28% от разброса машины игрока.
- Версия загрузки боевой логики обновлена для сброса браузерного кеша.

Затронутые файлы:

- `README.md`
- `index.html`
- `js/06-battle-logic.js`

Проверки:

- `node --check js/06-battle-logic.js` — успешно, синтаксических ошибок нет.
- `git diff --check` — успешно; найдены только информационные предупреждения Git о будущем преобразовании `README.md` из LF в CRLF.
- `data.csv` не изменялся, поэтому баланс-отчёты пересоздавать не требуется.

## Изменения `1.1.1.1v20`

- Нарисованы четыре оставшихся национальных флага для международных премиумов: Южная Корея, Иран, Египет и Бразилия.
- Каждый флаг создан отдельной растровой генерацией без программного или скриптового рисования и сохранён под уже подключённым в коде именем.
- После добавления этих файлов все 13 международных премиумов имеют собственные доступные PNG-флаги.

Затронутые файлы:

- `README.md`
- `img/flagi/south-korea.png`
- `img/flagi/iran.png`
- `img/flagi/egypt.png`
- `img/flagi/brazil.png`

Проверки:

- Сопоставления в `js/01-data.js` и `js/tankopedia-site.js` сверены: код ожидает именно `south-korea`, `iran`, `egypt` и `brazil`.
- Все четыре PNG визуально проверены: корейские триграммы и тхэгык, иранский герб и орнамент, египетский орёл, бразильские ромб, глобус, звёзды и надпись присутствуют.
- Файлы имеют единый размер `1536x1024` и соотношение сторон `3:2`, соответствующее существующим игровым флагам.
- `node tools/generate-balance-report.mjs` и `node tools/generate-data-md.mjs` — успешно; перед общим коммитом накопленных изменений отчёты повторно синхронизированы с `data.csv`.
- `node --check` для изменённых игровых и инструментальных JavaScript-файлов — без ошибок.

## Изменения `1.1.1.1v19`

- Нарисованы игровые текстуры для всех 13 международных премиумов X уровня: отдельная карточка собранной машины, корпус без башни и самостоятельная вращаемая башня со стволом вправо.
- Текстуры выполнены в едином ортографическом виде сверху с узнаваемыми силуэтами конкретных машин и прозрачным фоном.
- Для `Centurion Mk. 5/1 RAAC` сохранена совместимость с текущим именем из `data.csv`: часть после `/` размещена во вложенном пути `Centurion Mk. 5/1 RAAC.png`, который совпадает с URL, формируемым игрой.

Затронутые файлы:

- `README.md`
- `img/{tanki,korpus,bashnya}/Leopard C1.png`
- `img/{tanki,korpus,bashnya}/44M Tas.png`
- `img/{tanki,korpus,bashnya}/TR-85M1.png`
- `img/{tanki,korpus,bashnya}/Arjun Mk.1.png`
- `img/{tanki,korpus,bashnya}/K1.png`
- `img/{tanki,korpus,bashnya}/Leopard 2A4T1.png`
- `img/{tanki,korpus,bashnya}/Centurion Mk. 5/1 RAAC.png`
- `img/{tanki,korpus,bashnya}/Zulfiqar 3.png`
- `img/{tanki,korpus,bashnya}/Al-Khalid.png`
- `img/{tanki,korpus,bashnya}/Ramses II.png`
- `img/{tanki,korpus,bashnya}/Merkava Mk.1.png`
- `img/{tanki,korpus,bashnya}/EE-T1 Osório.png`
- `img/{tanki,korpus,bashnya}/Leopard 1BE.png`

Проверки:

- Автоматическая проверка 39 ожидаемых путей — найдены все 39 файлов, пропусков нет.
- Проверка PNG через Pillow — все 39 файлов имеют формат `RGBA`, содержат прозрачные пиксели и непустое изображение; размер каждого исходного игрового ассета `1536x1024`.
- Визуальная проверка полного комплекта `Merkava Mk.1` — собранная машина, корпус и башня корректно разделены; прозрачный фон и ориентация ствола сохранены.
- Сверка с существующим комплектом `Т-62` — соблюдены используемые игрой вид сверху, направление вправо и раздельные слои корпуса/башни.
- `data.csv` в этой версии не изменялся, поэтому `BALANCE_REPORT.md` и `data.md` повторно не создавались.

## Изменения `1.1.1.1v18`

- Добавлены 13 отдельных премиальных машин X уровня (`AC=2`) для Канады, Венгрии, Румынии, Индии, Южной Кореи, Турции, Австралии, Ирана, Пакистана, Египта, Израиля, Бразилии и Бельгии.
- Состав премиумов: `Leopard C1`, `44M Tas`, `TR-85M1`, `Arjun Mk.1`, `K1`, `Leopard 2A4T1`, `Centurion Mk. 5/1 RAAC`, `Zulfiqar 3`, `Al-Khalid`, `Ramses II`, `Merkava Mk.1`, `EE-T1 Osório`, `Leopard 1BE`.
- Машины получили полноценные рабочие характеристики X уровня, одиночные орудия, AH своей страны и статус премиумов; отдельные вкладки дерева для них не создавались.
- Добавлены сопоставления названий 13 стран с файлами флагов в игре и Танкопедии. Уже существующие `canada.png`, `israel.png` и `romania.png` используются сразу; для остальных стран ожидаются отдельные PNG.
- Добавлен идемпотентный инструмент `tools/add-international-premiums.mjs`, повторный запуск которого не создаёт дубли.

Затронутые файлы:

- `README.md`
- `data.csv`
- `data.md`
- `BALANCE_REPORT.md`
- `js/01-data.js`
- `js/tankopedia-site.js`
- `tools/add-international-premiums.mjs`
- `img/flagi/canada.png`
- `img/flagi/israel.png`
- `img/flagi/romania.png`

Проверки:

- `node tools/add-international-premiums.mjs` — успешно добавлены 13 уникальных премиумов; в CSV стало 448 строк, включая 447 корректных строк техники.
- `node tools/fill-detail-flags.mjs` — успешно, каждая из 13 стран получила одно значение AH.
- `node tools/generate-balance-report.mjs` — успешно, отчёт пересоздан для 447 танков.
- Ручная проверка новых строк в баланс-отчёте — все 13 машин имеют статус «В балансе» с индексами от `0,99` до `1,03`, новых выбросов нет.
- `node tools/generate-data-md.mjs` — успешно, `data.md` пересоздан для 447 танков.
- Синтаксис JavaScript и `git diff --check` — без ошибок.
- Текстуры корпусов, башен и карточек для новых машин пока отсутствуют; используются стандартные механизмы загрузки/резерва до добавления ассетов.

## Изменения `1.1.1.1v17`

- Полностью удалены оставшиеся экспериментальные ветки Югославии и Финляндии: 20 строк будущей техники, вкладки наций и сопоставления флагов.
- Удалён больше не нужный инструмент `tools/add-future-nation-branches.mjs`, чтобы снятые ветки нельзя было случайно восстановить повторным запуском.
- Панель дерева возвращена к 15 ячейкам: 14 основных наций и кнопка возврата.
- Пользовательские PNG-флаги удалённых наций, если они существуют, не удалялись.

Затронутые файлы:

- `README.md`
- `data.csv`
- `data.md`
- `BALANCE_REPORT.md`
- `css/styles.css`
- `js/01-data.js`
- `js/02-hangar-tech-tree.js`
- `js/tankopedia-site.js`
- `tools/add-future-nation-branches.mjs` — удалён

Проверки:

- Из `data.csv` удалены все значения наций `Югославия` и `Финляндия`; осталось 435 CSV-строк, включая 434 корректные строки техники.
- `node tools/fill-detail-flags.mjs` — успешно, в распределении AH удалённых наций нет.
- `node tools/generate-balance-report.mjs` — успешно, отчёт пересоздан для 434 танков.
- `node tools/generate-data-md.mjs` — успешно, `data.md` пересоздан для 434 танков.
- Конфигурация дерева содержит 14 наций, `--nation-tab-count: 15` учитывает кнопку возврата.
- Синтаксис JavaScript и `git diff --check` — без ошибок.

## Изменения `1.1.1.1v16`

- Новые ветки проверены по строгому временному условию: первая машина выпущена до 1935 года, последняя — до 1990 года.
- Полностью удалены ветки Израиля, Румынии и Венгрии вместе с 30 строками будущей техники, вкладками и активными сопоставлениями флагов.
- Израиль исключён из-за `Hotchkiss H39` производства после 1935 года; Румыния — из-за серийного `R-1` 1937 года; Венгрия — из-за готового `Straussler V-3` 1935 года при требовании строго более ранней даты.
- Сохранены Югославия (`Renault FT`, 1917 → `M-84A`, 1988) и Финляндия (`Vickers 6-ton`, 1928 → базовая модификация `Leopard 2A4`, 1980-е).
- Инструмент генерации теперь поддерживает только две прошедшие аудит ветки и при запуске удаляет ранее снятые машины КНДР, Израиля, Румынии и Венгрии.
- Панель дерева уменьшена до 17 ячеек: 16 наций и кнопка возврата. Пользовательские файлы флагов удалённых наций сохранены на диске без использования.

Затронутые файлы:

- `README.md`
- `data.csv`
- `data.md`
- `BALANCE_REPORT.md`
- `css/styles.css`
- `js/01-data.js`
- `js/02-hangar-tech-tree.js`
- `js/tankopedia-site.js`
- `tools/add-future-nation-branches.mjs`

Проверки:

- Исторический аудит крайних машин — условие прошли только Югославия и Финляндия.
- `node tools/add-future-nation-branches.mjs` — повторный запуск добавил 0 дублей, удалённые ветки не восстановились; осталось 455 CSV-строк, включая 454 корректные строки техники.
- `node tools/fill-detail-flags.mjs` — успешно, в AH осталось по 10 строк Югославии и Финляндии; значений удалённых наций нет.
- `node tools/generate-balance-report.mjs` — успешно, отчёт пересоздан для 454 танков; 20 новых строк с `AC=0` не участвуют в боевом подборе.
- `node tools/generate-data-md.mjs` — успешно, `data.md` пересоздан для 454 танков.
- Синтаксис JavaScript и `git diff --check` — без ошибок.

## Изменения `1.1.1.1v15`

- В `data.csv` добавлены пять полных веток будущей техники без боевых характеристик: Израиль, Румыния, Югославия, Венгрия и Финляндия — по 10 машин I–X уровней.
- Все новые машины имеют `AC=0`: отображаются как будущие в дереве, но не участвуют в боях, магазине и контейнерах.
- У каждой ветки техника VIII–X уровней имеет единый класс. Связи ПТ не переходят обратно на другой класс; отдельные короткие линии ПТ завершаются без некорректного продолжения.
- Добавлены вкладки пяти наций и сопоставления флагов. Существующие `img/flagi/israel.png` и `img/flagi/romania.png` подключены; остальные вкладки ожидают `yugoslavia.png`, `hungary.png` и `finland.png`.
- Первоначально подготовленная ветка КНДР полностью удалена по решению автора проекта: удалены 10 строк, вкладка, сопоставление флага и описание в инструменте.
- Добавлен идемпотентный инструмент `tools/add-future-nation-branches.mjs`, который восстанавливает пять веток, удаляет снятую ветку КНДР и проверяет структуру верхних уровней и переходы ПТ.
- Число ячеек панели наций увеличено до 20: 19 наций и кнопка возврата.

Затронутые файлы:

- `README.md`
- `data.csv`
- `data.md`
- `BALANCE_REPORT.md`
- `css/styles.css`
- `js/01-data.js`
- `js/02-hangar-tech-tree.js`
- `js/tankopedia-site.js`
- `tools/add-future-nation-branches.mjs`
- `img/flagi/israel.png`
- `img/flagi/romania.png`

Проверки:

- `node tools/add-future-nation-branches.mjs` — повторный запуск добавил 0 дублей, структура пяти веток прошла встроенную проверку; в CSV осталось 485 строк, включая 484 корректные строки техники.
- `node tools/fill-detail-flags.mjs` — успешно, по 10 машин получили AH каждой новой нации; значений КНДР не осталось.
- `node tools/generate-balance-report.mjs` — успешно, отчёт пересоздан для 484 танков; 50 новых строк с `AC=0` не участвуют в текущем подборе и не меняют боевые выбросы.
- `node tools/generate-data-md.mjs` — успешно, `data.md` пересоздан для 484 танков.
- Проверено: в каждой новой нации ровно 10 машин, классы VIII–X совпадают, переходов с ПТ на иной класс нет.
- Синтаксис изменённых JavaScript-файлов и `git diff --check` — без ошибок.

## Изменения `1.1.1.1v14`

- При открытии дерева развития автоматически выбирается нация текущего танка вместо сохранённой ранее или стандартной советской вкладки.
- Первичный фон открытой ветки учитывает AH выбранного танка: `Российская империя`, `ФРГ`, `Веймарская республика`, `Австро-Венгрия`, современная и имперская Япония используют свои детализирующие флаги.
- Для выбранной советской техники значения `РСФСР`, `УССР` и `БССР` сворачиваются в общий фон СССР, сохраняя единую советскую ветку.
- Вкладки наций по-прежнему показывают и после ручного переключения открывают основные национальные флаги.

Затронутые файлы:

- `README.md`
- `js/02-hangar-tech-tree.js`
- `js/04-ui-developer.js`

Проверки:

- `node --check js/02-hangar-tech-tree.js` и `node --check js/04-ui-developer.js` — синтаксис корректен.
- Статически проверены сценарии: республика СССР → `sssr.png`, `Российская империя` → `russian-empire.png`, ручное переключение вкладки → основной файл из конфигурации нации.
- `git diff --check` — ошибок форматирования нет.
- `data.csv` не изменялся, поэтому `BALANCE_REPORT.md` и `data.md` повторно не пересоздавались.

## Изменения `1.1.1.1v13`

- В `data.csv` добавлен премиальный бронеавтомобиль I уровня `Руссо-Балт тип C` в нацию СССР с детализирующим флагом `Российская империя`.
- Машина получила 180 HP, три типа пулемётных лент, магазин на 24 патрона, один снаряд за выстрел, перезарядку 6 секунд, броню 7 и размер 2.
- Инструмент заполнения AH сохраняет для `Руссо-Балт тип C` флаг Российской империи при повторной генерации.
- Подключены добавленные пользователем изображения корпуса и общей карточки машины.

Затронутые файлы:

- `README.md`
- `data.csv`
- `data.md`
- `BALANCE_REPORT.md`
- `tools/fill-detail-flags.mjs`
- `img/korpus/Руссо-Балт тип C.png`
- `img/tanki/Руссо-Балт тип C.png`

Проверки:

- `node tools/fill-detail-flags.mjs` — успешно, строка сохраняет `Российская империя`; всего обработано 434 корректных строки техники и одна служебная пустая CSV-строка.
- `node tools/generate-balance-report.mjs` — успешно, отчёт пересоздан для 434 танков.
- После первичного расчёта пулемётный залп снижен с двух снарядов до одного: итоговый индекс `0,88`, статус «В балансе», замечаний по машине нет.
- `node tools/generate-data-md.mjs` — успешно, `data.md` пересоздан для 434 танков.
- Проверено наличие `img/korpus/Руссо-Балт тип C.png` и `img/tanki/Руссо-Балт тип C.png`; отдельный файл башни пользователем не добавлен.
- Синтаксис изменённых JavaScript-инструментов и `git diff --check` — без ошибок.

## Изменения `1.1.1.1v12`

- Советская техника в AH разделена по республике разработки/основного завода без расшифровки аббревиатур: 125 машин получили `РСФСР`, 12 уверенно связанных с Харьковом машин — `УССР`; четыре постсоветские машины сохраняют `Россия`.
- Значения `РСФСР`, `УССР` и `БССР` подключены к будущим ассетам `rsfsr.png`, `ussr-ukraine.png` и `ussr-belarus.png`. В текущей таблице уверенных машин сборки БССР не найдено, поэтому значение `БССР` пока не назначено.
- Старый флаг `img/flagi/japan.png` переименован в `img/flagi/japanese-empire.png`; все существующие фоны имперского периода и японского события переведены на новый путь.
- Японская техника разделена в AH: 20 довоенных и военных машин используют `Японская империя`, восемь послевоенных машин — `Япония` и новый современный ассет `img/flagi/japan.png`.
- Инструмент массового заполнения AH обновлён, чтобы повторно применять разделение советских республик и двух периодов Японии.

Затронутые файлы:

- `README.md`
- `data.csv`
- `css/styles.css`
- `js/01-data.js`
- `js/02-hangar-tech-tree.js`
- `js/tankopedia-site.js`
- `tools/fill-detail-flags.mjs`
- `img/flagi/japanese-empire.png` — новое имя существующего ассета
- `img/flagi/japan.png` — добавленный пользователем современный флаг Японии

Проверки:

- `node tools/fill-detail-flags.mjs` — успешно; AH распределён между 125 `РСФСР`, 12 `УССР`, 4 `Россия`, 20 `Японская империя` и 8 `Япония`, остальные ранее настроенные нации сохранены.
- `node tools/generate-balance-report.mjs` — успешно, отчёт пересоздан для 433 танков; баланс-выбросы не изменились.
- `node tools/generate-data-md.mjs` — успешно, `data.md` пересоздан для 433 танков.
- Проверены оба японских ассета: имперские ссылки используют `japanese-empire.png`, современная `Япония` использует добавленный `japan.png`.
- Проверка JavaScript и `git diff --check` — успешно.

## Изменения `1.1.1.1v11`

- Столбец `AH` заполнен у всех 433 строк техники: обычные машины получают название своей нации, а исторические исключения — отдельное имя фонового флага.
- Для немецкой техники применена заданная схема: `Германия` до 1919 года, `Веймарская республика` для периода 1919–1945 годов и `ФРГ` после 1945 года, включая технику ГДР.
- Семь послевоенных западногерманских машин помечены как `ФРГ`, единственная машина до 1919 года — как `Германия`, остальные 57 немецких машин — как `Веймарская республика`.
- Австрийская машина I уровня `Lancia IZM` помечена как `Австро-Венгрия` по решению автора проекта.
- `БТР-82`, `БТР-90 Росток`, `БТР-90М` и `К-17 Бумеранг` получили российский флаг; остальная советская ветка сохраняет `СССР`.
- Добавлен повторяемый инструмент `tools/fill-detail-flags.mjs`, который восстанавливает принятую раскладку AH после дальнейшего редактирования таблицы.

Затронутые файлы:

- `README.md`
- `data.csv`
- `tools/fill-detail-flags.mjs`

Проверки:

- `node tools/fill-detail-flags.mjs` — успешно обработаны 434 CSV-строки, включая 433 корректные строки техники; у каждой строки техники заполнен AH.
- Ручная сверка исключений — подтверждены `Германия` у `G.Pz. Mk. VI (e)`, `ФРГ` у семи послевоенных немецких машин, `Россия` у четырёх постсоветских машин и `Австро-Венгрия` у `Lancia IZM`.
- `node tools/generate-balance-report.mjs` — успешно, отчёт пересоздан для 433 танков; баланс-выбросы не изменились, поскольку AH отвечает только за фон.
- `node tools/generate-data-md.mjs` — успешно, `data.md` пересоздан для 433 танков.
- `node --check tools/fill-detail-flags.mjs` — синтаксис корректен.

## Изменения `1.1.1.1v10`

- Для столбца `AH` добавлено сопоставление `Австро-Венгрия` с ассетом `img/flagi/austro-hungary.png` в игре и Танкопедии.
- Проведён ручной исторический аудит названий техники и уже заполненных значений AH; результат передан отдельно с разделением достоверных, спорных и вымышленных проектов.
- После сохранения нового значения AH в `data.csv` пересозданы обязательные отчёты.

Затронутые файлы:

- `README.md`
- `data.csv`
- `data.md`
- `js/01-data.js`
- `js/tankopedia-site.js`
- `img/flagi/austro-hungary.png`

Проверки:

- `node tools/generate-balance-report.mjs` — успешно, отчёт пересоздан для 433 танков; баланс-выбросы не изменились, поскольку AH влияет только на фон.
- `node tools/generate-data-md.mjs` — успешно, `data.md` пересоздан для 433 танков.
- `node --check js/01-data.js` и `node --check js/tankopedia-site.js` — синтаксис корректен.
- Проверено наличие `img/flagi/austro-hungary.png` и совпадение русского значения AH с новым сопоставлением без учёта регистра.

## Изменения `1.1.1.1v9`

- Столбец `AH` (`detailFlag`) в `data.csv` подключён как необязательное переопределение фонового флага танка в ангаре и Танкопедии. Если AH пуст, по-прежнему используется флаг нации из столбца `I`; на нацию, дерево развития и текстовые сведения новое поле не влияет.
- Добавлены сопоставления исторических флагов для значений `Россия`, `Российская империя`, `Веймарская республика` и `ФРГ`.
- В настройках добавлена пометка: флаг Веймарской республики с железным крестом используется для отображения эпохи.
- Генератор `data.md` и справочник столбцов дополнены колонкой AH.
- Сохранены внесённые в `data.csv` изменения размеров первых четырёх машин австрийской ветки и добавленные ассеты исторических флагов.

Затронутые файлы:

- `README.md`
- `data.csv`
- `data.md`
- `js/01-data.js`
- `js/02-hangar-tech-tree.js`
- `js/04-ui-developer.js`
- `js/tankopedia-site.js`
- `tools/generate-data-md.mjs`
- `img/flagi/austro-hungary.png`
- `img/flagi/frg.png`
- `img/flagi/german-respublik.png`
- `img/flagi/russia.png`
- `img/flagi/russian-empire.png`

Проверки:

- `node tools/generate-balance-report.mjs` — успешно, отчёт пересоздан для 433 танков; содержимое `BALANCE_REPORT.md` не изменилось.
- `node tools/generate-data-md.mjs` — успешно, `data.md` пересоздан для 433 танков и содержит описание AH.
- Ручная проверка баланс-выбросов — новых или изменившихся выбросов нет: правки `data.csv` затронули только размер техники и не изменили показатели баланс-отчёта.
- Синтаксическая и файловая проверка JavaScript и исторических флагов — успешно.

## Запуск

Проект работает как статическая браузерная игра. Запускать лучше через локальный HTTP-сервер, потому что `data.csv` загружается через `fetch`.

```powershell
python -m http.server 8000
```

После запуска открыть:

```text
http://localhost:8000/
```

## Структура

- `index.html` - базовая HTML-структура и подключение файлов.
- `css/styles.css` - все стили интерфейса.
- `data.csv` - данные танков, экономики, снарядов и доступности.
- `img/` - изображения интерфейса и танков.
- `js/00-state.js` - глобальное состояние, режимы боя, настройки, константы.
- `js/01-data.js` - загрузка CSV, сохранения, cookies/localStorage, экипаж.
- `js/02-hangar-tech-tree.js` - ангар, дерево техники, сравнение танков, экипаж.
- `js/03-events-store-profile.js` - события, ежедневные награды и задания, магазин, профиль.
- `js/04-ui-developer.js` - режимы боя, настройки, overlay, developer mode.
- `js/05-battle-maps.js` - карты боя.
- `js/06-battle-logic.js` - логика боя, награды, выживание, статистика.
- `js/07-battle-render.js` - отрисовка боя и HUD.
- `js/08-startup.js` - старт игры, запуск/остановка боя, управление.

## Изменения 1.1.1.1v8

Изменено:

- Барабанные орудия типа `Y = 2`, включая австрийские пулемёты, теперь продолжают стрелять при удержании кнопки огня; повторно нажимать ЛКМ для каждого снаряда не требуется.
- Автоматический огонь при удержании работает одинаково для ЛКМ, мобильной кнопки «Огонь» и клавиши `Пробел`.
- После отпускания кнопки или клавиши автоматический огонь немедленно прекращается; одиночные орудия по-прежнему делают только один выстрел за нажатие.
- Параметры загрузки боевой логики и обработчиков управления обновлены до `1.1.1.1v8` для сброса браузерного кеша.

Проверено:

- `node --check js/06-battle-logic.js` — синтаксических ошибок нет.
- `node --check js/08-startup.js` — синтаксических ошибок нет.
- Проверено включение удерживаемого огня только для потоковых снарядов и барабанов `Y = 2`; обычные и дозаряжаемые орудия не переведены в автоматический режим.
- Проверен сброс удерживаемого огня при отпускании ЛКМ, `Пробела`, мобильной кнопки и при уходе указателя за пределы боя.
- `git diff --check` — ошибок форматирования не обнаружено.

Файлы, изменённые в версии `1.1.1.1v8`:

- `README.md`
- `index.html`
- `js/06-battle-logic.js`
- `js/08-startup.js`

## Изменения 1.1.1.1v7

Добавлено:

- Для снарядов типа `Пулемёт` реализован специальный барабанный цикл: `0,05` секунды между пулями и полная перезарядка по значению `Q` только после опустошения магазина.
- Размер пулемётного магазина полностью управляется колонкой `AA` (`clipSize`) в `data.csv`; расчёт боекомплекта гарантирует минимум три полных магазина каждого типа пулемётных патронов.

Изменено:

- `Lancia IZM` получил 30-патронный магазин, урон `1 / 1 / 2` за пулю и полную перезарядку 6 секунд.
- `Austro-Daimler Panzerwagen` получил 40-патронный магазин, урон `2 / 2 / 3` за пулю и полную перезарядку 6 секунд.
- Обычные барабанные орудия сохраняют прежний интервал 1 секунда; новая скорострельность применяется только при типе снаряда `Пулемёт`.
- Формулы баланс-отчёта обновлены для учёта пулемётного интервала `0,05` секунды, а справочник `data.md` — для описания новой механики.
- Параметры загрузки `js/01-data.js` и `js/06-battle-logic.js` обновлены до `1.1.1.1v7` для сброса браузерного кеша.

Проверено:

- `node --check js/01-data.js` — синтаксических ошибок нет.
- `node --check js/06-battle-logic.js` — синтаксических ошибок нет.
- `node --check tools/generate-balance-report.mjs` — синтаксических ошибок нет.
- `node --check tools/generate-data-md.mjs` — синтаксических ошибок нет.
- `node tools/generate-balance-report.mjs` — отчёт успешно пересоздан для 433 корректных машин; `Lancia IZM` и `Austro-Daimler Panzerwagen` сохранили вердикт «В балансе» с индексами `0,95` и `0,83`.
- `node tools/generate-data-md.mjs` — `data.md` успешно пересоздан и отражает магазины на 30/40 патронов, КД 6 секунд и сниженный урон пули.
- Проверено, что боекомплект пулемётной машины содержит не менее трёх полных магазинов каждого доступного типа патронов.
- `git diff --check` — ошибок форматирования не обнаружено.

Файлы, изменённые в версии `1.1.1.1v7`:

- `README.md`
- `data.csv`
- `BALANCE_REPORT.md`
- `data.md`
- `index.html`
- `js/01-data.js`
- `js/06-battle-logic.js`
- `tools/generate-balance-report.mjs`
- `tools/generate-data-md.mjs`

## Изменения 1.1.1.1v6

Изменено:

- Скорость поворота корпуса всех 10 машин австрийской ветки заметно увеличена за счёт снижения задержки поворота.
- Прогрессия задержки исправлена: вместо ухудшения от `0,018` до `0,025` теперь она уменьшается от `0,012` на `Lancia IZM` до `0,0075` на `Pandur II (8×8)`; старшие машины поворачивают быстрее младших.

Проверено:

- `node tools/generate-balance-report.mjs` — отчёт успешно пересоздан для 433 корректных машин; содержимое `BALANCE_REPORT.md` не изменилось, поскольку индекс отчёта не учитывает задержку поворота корпуса.
- Ручная проверка подтвердила вердикт «В балансе» для всех 10 машин Австрии с прежним диапазоном индексов `0,80–1,10`.
- `node tools/generate-data-md.mjs` — `data.md` успешно пересоздан и содержит новую последовательную прогрессию поворота `0,012–0,0075`.
- `git diff --check` — ошибок форматирования не обнаружено.

Файлы, изменённые в версии `1.1.1.1v6`:

- `README.md`
- `data.csv`
- `data.md`

## Изменения 1.1.1.1v5

Изменено:

- Серебряная стоимость австрийской ветки приведена к облегчённой шкале для классов `КТ` и `БТР`: от `5 000` серебра на I уровне до `4 500 000` на X.
- Промежуточные цены последовательно установлены в `30 000`, `120 000`, `350 000`, `800 000`, `1 300 000`, `2 000 000`, `2 800 000` и `3 600 000` серебра для II–IX уровней.
- Стоимость исследования за опыт не менялась и остаётся облегчённой: от `800` на II уровне до `100 000` на X.

Проверено:

- `node tools/generate-balance-report.mjs` — отчёт успешно пересоздан для 433 корректных машин; содержимое `BALANCE_REPORT.md` не изменилось, поскольку боевые параметры не затрагивались.
- Ручная проверка подтвердила сохранение вердикта «В балансе» для всех 10 австрийских машин; изменение экономики не затронуло боевые индексы.
- `node tools/generate-data-md.mjs` — `data.md` успешно пересоздан; в таблице отражена шкала от `5 000` до `4 500 000` серебра.
- `git diff --check` — ошибок форматирования не обнаружено.

Файлы, изменённые в версии `1.1.1.1v5`:

- `README.md`
- `data.csv`
- `data.md`

## Изменения 1.1.1.1v4

Добавлено:

- Последние 10 машин `data.csv` оформлены как полноценная австрийская ветка I–X уровней: `Lancia IZM`, `Austro-Daimler Panzerwagen`, `Steyr ADGZ`, `Sd.Kfz. 221`, `ADGZ (M35)`, `Saurer 3K 3H`, `Saurer 4K 4FA`, `4K 7FA G127`, `Pandur I (6×6)` и `Pandur II (8×8)`.
- Для каждой машины добавлены типы и параметры снарядов, урон, пробитие, HP, перезарядка, динамика, броня, шанс пробития, разброс, размер, УВН и экономика исследования/покупки.

Изменено:

- Австрийская линия последовательно развивается от колёсной техники класса `КТ` I–V уровней к быстрым бронетранспортёрам класса `БТР` VI–X уровней.
- `Austro-Daimler Panzerwagen` дополнительно усилен после первого пересчёта: итоговый индекс баланса поднят с `0,66` до `0,83`.

Проверено:

- `node tools/generate-balance-report.mjs` — `BALANCE_REPORT.md` успешно пересоздан для 433 корректных машин.
- Выполнена ручная проверка всех 10 австрийских машин: каждая получила вердикт «В балансе», диапазон индексов — `0,80–1,10`.
- Связи исследования образуют непрерывную линию I–X уровней; цены опыта и серебра последовательно растут по уровням.
- `node tools/generate-data-md.mjs` — `data.md` успешно пересоздан для 433 корректных машин.
- `git diff --check` — ошибок форматирования не обнаружено.

Файлы, изменённые в версии `1.1.1.1v4`:

- `README.md`
- `data.csv`
- `BALANCE_REPORT.md`
- `data.md`

## Изменения 1.1.1.1v3

Изменено:

- Клавиша `Tab` теперь скрывает и возвращает весь боевой HUD: панели здоровья и модулей, составы команд, маркеры и полосы здоровья техники, мини-карту, обучение, боеприпасы, расходники, перезарядку, мобильное управление и кнопку выхода.
- Указатель мыши остаётся видимым и продолжает управлять башней при скрытом интерфейсе.
- Состояние HUD сбрасывается при каждом запуске и завершении боя, поэтому новый бой всегда начинается с видимым интерфейсом.
- Текст боевого обучения обновлён в соответствии с новым поведением `Tab`.
- Параметры загрузки изменённых стилей и скриптов обновлены до `1.1.1.1v3` для сброса браузерного кеша.

Проверено:

- `node --check js/00-state.js` — синтаксических ошибок нет.
- `node --check js/07-battle-render.js` — синтаксических ошибок нет.
- `node --check js/08-startup.js` — синтаксических ошибок нет.
- Проверено раздельное скрытие canvas-HUD и HTML-HUD классом `hudHidden`; игровой canvas и указатель мыши под это правило не попадают.
- Проверено восстановление видимости HUD при повторном нажатии `Tab`, запуске нового боя и выходе из боя.
- `git diff --check` — ошибок форматирования не обнаружено.

Файлы, изменённые в версии `1.1.1.1v3`:

- `README.md`
- `css/styles.css`
- `index.html`
- `js/00-state.js`
- `js/07-battle-render.js`
- `js/08-startup.js`

## Изменения 1.1.1.1v2

Изменено:

- В экспериментальной ветке `redesing` полностью восстановлено исходное отображение списка наций и фоновых флагов дерева техники.
- Нижняя панель боевого HUD оформлена как компактная полупрозрачная консоль с адаптивной горизонтальной прокруткой.
- Снаряды, переключатель режима огня и расходники получили отдельные визуальные типы; выбранный слот, недоступное состояние и наведение стали заметнее.
- Клавиши управления вынесены в небольшие бейджи, а индикатор перезарядки и кнопка выхода получили обновлённый стиль.
- Параметры загрузки CSS и боевой логики обновлены до `1.1.1.1v2` для сброса браузерного кеша.

Проверено:

- `node --check js/06-battle-logic.js` — синтаксических ошибок нет.
- Проверено разделение слотов HUD на классы `shell`, `mode` и `consumable` без изменения обработчиков выбора и применения.
- Проверена адаптивная ширина нижней панели и возможность горизонтальной прокрутки при нехватке места.
- `git diff --check` — ошибок форматирования не обнаружено.

Файлы, изменённые в версии `1.1.1.1v2`:

- `README.md`
- `css/styles.css`
- `index.html`
- `js/06-battle-logic.js`

## Изменения 1.1.1.1v1

Добавлено:

- Австрия добавлена в список наций и отображается отдельной вкладкой дерева техники с уже подготовленным флагом `img/flagi/Austria.png`.
- Для названий `австрия` и `Austria` добавлено единое сопоставление изображения флага в игре и Танкопедии.

Изменено:

- Текущая версия проекта указана как релиз `1.1.1.1v1`; обновлены параметры загрузки изменённых игровых скриптов для сброса браузерного кеша.
- Расчёт ширины верхней панели дерева обновлён до 15 ячеек: 14 вкладок наций и кнопка «Назад», поэтому вкладка Австрии больше не обрезается за правой границей.

Проверено:

- `node --check js/01-data.js` — синтаксических ошибок нет.
- `node --check js/02-hangar-tech-tree.js` — синтаксических ошибок нет.
- `node --check js/tankopedia-site.js` — синтаксических ошибок нет.
- Проверено наличие `img/flagi/Austria.png`; конфигурация Австрии ссылается на существующий ассет.
- Проверена компоновка панели: `--nation-tab-count: 15` соответствует 14 нациям и одной кнопке «Назад».
- `data.csv` не изменялся, поэтому баланс-отчёт и `data.md` не пересоздавались.
- `git diff --check` — ошибок форматирования не обнаружено.

Файлы, изменённые в версии `1.1.1.1v1`:

- `README.md`
- `css/styles.css`
- `index.html`
- `img/flagi/Austria.png`
- `js/01-data.js`
- `js/02-hangar-tech-tree.js`
- `js/tankopedia-site.js`

## Изменения 1.1.1.0r

Добавлено:

- Опубликована расширенная статья Games Studio с полным описанием обновления `1.1.1.0`, назначением японской тяжёлой ветки, всеми этапами события и точными изменениями каждой машины ребаланса: [Tanks Wars 1.1.1.0 — полное описание обновления](https://telegra.ph/Tanks-Wars-1110--polnoe-opisanie-obnovleniya-07-31).

Изменено:

- Проведён полный ребаланс 19 прокачиваемых танков мировой нации; премиумные, контейнерные и коллекционные машины этой нации не изменялись.
- Для лёгких танков `Т-113 пуля`, `Т-274 вано` и `Т-578 алмаз` скорректирована перезарядка, чтобы убрать завышенный темп нанесения урона на IV–VI уровнях.
- Линия ПТ `К.Р.` получила соответствующий уровням запас прочности, а `К.Р.-17 А.К.` — достаточную среднюю броню при сохранении роли уязвимой огневой поддержки.
- Линия тяжёлых танков `НОРТ` получила последовательные значения HP, брони, пробития, шанса пробития и перезарядки; устранён провальный эффективный DPM на VII–X уровнях.

Проверено:

- `node tools/generate-balance-report.mjs` — отчёт пересоздан; все 19 прокачиваемых машин мировой нации имеют итоговый вердикт «В балансе».
- Премиумные и коллекционные машины мировой нации (`КВ-2 НГ`, `Т-34-85 НГ`, `Nexor`, `ПОРОГ`) не изменены.
- `node tools/generate-data-md.mjs` — русскоязычное представление данных успешно пересоздано.
- `git diff --check` — ошибок форматирования не обнаружено.

Файлы, изменённые в версии `1.1.1.0r`:

- `README.md`
- `data.csv`
- `BALANCE_REPORT.md`
- `data.md`

## Изменения 1.1.1.0v17

Изменено:

- Фон карточки и детального экрана события «Эксперимент 120 тонн» заменён на существующий проектный флаг Японской империи `img/flagi/japan.png`.
- Сохранены затемняющие градиенты для читаемости текста и отдельное адаптивное кадрирование на мобильных экранах.
- Обновлена версия подключения CSS для сброса браузерного кеша.

Проверено:

- Ассет `img/flagi/japan.png` просмотрен и корректно подключён в четырёх вариантах фона события.
- `git diff --check` — ошибок форматирования не обнаружено.

Файлы, изменённые в версии `1.1.1.0v17`:

- `README.md`
- `index.html`
- `css/styles.css`

## Изменения 1.1.1.0v16

Добавлено:

- Ограниченное событие «Эксперимент 120 тонн», активное с 1 по 7 августа 2026 года включительно.
- В списке событий появилась крупная интерактивная карточка с датами, общим прогрессом и переходом на отдельный экран подробностей.
- Детальный экран события с фоном в виде японского флага, пятью инженерными этапами, шкалами прогресса, наградами этапов, главным призом и кнопкой «К событиям» для возврата.
- Каждый завершённый этап выдаёт 100 чертежей и 50 000 серебра; за все пять этапов выдаются коллекционный `O-I Experimental` и 500 золота.
- Прогресс события автоматически учитывает бои, заблокированный урон, победы, нанесённый урон и уничтоженную технику во всех боевых режимах, кроме тренировки и тест-драйва.

Изменено:

- `O-I Experimental` переведён в коллекционную категорию и удалён из обычного премиум-магазина.
- Обновлены версии подключений CSS и логики события/боя для сброса браузерного кеша.

Проверено:

- `node --check js/00-state.js`, `node --check js/03-events-store-profile.js` и `node --check js/06-battle-logic.js` — синтаксических ошибок нет.
- `node tools/generate-balance-report.mjs` и `node tools/generate-data-md.mjs` — производные отчёты пересозданы; изменение категории не повлияло на боевые характеристики и выбросы.
- `git diff --check` — ошибок форматирования не обнаружено.

Файлы, изменённые в версии `1.1.1.0v16`:

- `README.md`
- `index.html`
- `css/styles.css`
- `data.csv`
- `data.md`
- `js/00-state.js`
- `js/03-events-store-profile.js`
- `js/06-battle-logic.js`

## Изменения 1.1.1.0v15

Добавлено:

- Для `Chi-He` добавлены изображения танка, корпуса и башни.

Проверено:

- Все три PNG-файла присутствуют в соответствующих каталогах ассетов.
- `git diff --check` — ошибок форматирования не обнаружено.

Файлы, изменённые в версии `1.1.1.0v15`:

- `README.md`
- `img/bashnya/Chi-He.png`
- `img/korpus/Chi-He.png`
- `img/tanki/Chi-He.png`

## Изменения 1.1.1.0v14

Изменено:

- Цена одного «Контейнера ИС-4» увеличена до 1000 золота.
- В карточку «Контейнера ИС-4» добавлено открытие набора из 10 контейнеров за 7000 золота; каждый контейнер в наборе независимо имеет 10% шанс выдать один из трёх танков.
- При массовом открытии одинаковые ресурсные награды суммируются на итоговом экране, а выпавшие танки показываются отдельно.
- Обновлены версии подключений CSS и изменённых скриптов для сброса браузерного кеша.

Проверено:

- `node --check js/00-state.js` и `node --check js/03-events-store-profile.js` — синтаксических ошибок нет.
- `git diff --check` — ошибок форматирования не обнаружено.

Файлы, изменённые в версии `1.1.1.0v14`:

- `README.md`
- `index.html`
- `css/styles.css`
- `js/00-state.js`
- `js/03-events-store-profile.js`

## Изменения 1.1.1.0v13

Добавлено:

- В раздел магазина «Контейнеры» добавлен «Контейнер ИС-4» с 10% шансом получить один из танков: `ИС-4М`, `ИС-4К` или `ИС-4 С-70`; при невыпадении танка контейнер выдаёт три обычные ресурсные награды.

Изменено:

- `ИС-4М`, `ИС-4К` и `ИС-4 С-70` переведены из премиум/контейнерной категории в коллекционную.
- Продажа любой коллекционной техники теперь запрещена общей логикой ангара; вместо цены кнопка показывает «Продажа недоступна».
- Карточки контейнеров переведены на общую конфигурацию с индивидуальным названием, ценой, шансом и набором танков.
- Обновлены версии подключений изменённых скриптов для сброса браузерного кеша.

Проверено:

- `node --check js/00-state.js`, `node --check js/02-hangar-tech-tree.js` и `node --check js/03-events-store-profile.js` — синтаксических ошибок нет.
- `node tools/generate-balance-report.mjs` и `node tools/generate-data-md.mjs` — отчёты успешно пересозданы; изменение категории не меняет боевые характеристики и не добавило боевых выбросов.
- `git diff --check` — ошибок форматирования не обнаружено.

Файлы, изменённые в версии `1.1.1.0v13`:

- `README.md`
- `index.html`
- `data.csv`
- `data.md`
- `js/00-state.js`
- `js/02-hangar-tech-tree.js`
- `js/03-events-store-profile.js`

## Изменения 1.1.1.0v12

Добавлено:

- В магазине появился отдельный расширяемый раздел «Контейнеры» со своей сеткой карточек.

Изменено:

- Существующий контейнер переименован в «Все из всех»; новое название показывается в магазине, на сцене готового контейнера и в заголовке полученных призов.
- Обновлены версии подключений CSS и скриптов магазина для сброса браузерного кеша.

Проверено:

- `node --check js/00-state.js` и `node --check js/03-events-store-profile.js` — синтаксических ошибок нет.
- `git diff --check` — ошибок форматирования не обнаружено.

Файлы, изменённые в версии `1.1.1.0v12`:

- `README.md`
- `index.html`
- `css/styles.css`
- `js/00-state.js`
- `js/03-events-store-profile.js`

## Изменения 1.1.1.0v11

Исправлено:

- Сцена открытия контейнера теперь размещается внутри экранного слоя магазина и полностью перекрывает его без остаточного затемнения.
- На время открытия контейнера скрываются верхняя панель с кнопкой запуска боя, боковые кнопки, карусель танков и кнопка возврата; после нажатия «Продолжить» интерфейс восстанавливается.
- Обновлены версии подключений CSS и логики магазина для сброса браузерного кеша.

Проверено:

- `node --check js/03-events-store-profile.js` — синтаксических ошибок нет.
- `git diff --check` — ошибок форматирования не обнаружено.

Файлы, изменённые в версии `1.1.1.0v11`:

- `README.md`
- `index.html`
- `css/styles.css`
- `js/03-events-store-profile.js`

## Изменения 1.1.1.0v10

Добавлено:

- Для `Chi-Ha` добавлены изображения танка, корпуса и башни.

Проверено:

- Все три PNG-файла присутствуют в соответствующих каталогах ассетов.
- `git diff --check` — ошибок форматирования не обнаружено.

Файлы, изменённые в версии `1.1.1.0v10`:

- `README.md`
- `img/bashnya/Chi-Ha.png`
- `img/korpus/Chi-Ha.png`
- `img/tanki/Chi-Ha.png`

## Изменения 1.1.1.0v9

Изменено:

- Обновлены изображения танка, корпуса и башни `ИС-4`.

Проверено:

- Все три обновлённых PNG-файла присутствуют в соответствующих каталогах ассетов.
- `git diff --check` — ошибок форматирования не обнаружено.

Файлы, изменённые в версии `1.1.1.0v9`:

- `README.md`
- `img/bashnya/ИС-4.png`
- `img/korpus/ИС-4.png`
- `img/tanki/ИС-4.png`

## Изменения 1.1.1.0v8

Добавлено:

- Для `Mitsu 108` добавлены изображения танка, корпуса и башни.

Проверено:

- Все три PNG-файла присутствуют в соответствующих каталогах ассетов.

Файлы, изменённые в версии `1.1.1.0v8`:

- `README.md`
- `img/bashnya/Mitsu 108.png`
- `img/korpus/Mitsu 108.png`
- `img/tanki/Mitsu 108.png`

## Изменения 1.1.1.0v7

Изменено:

- Для `Chi-Ha`, `Chi-He` и `Mitsu 108` добавлена стоимость исследования в опыте и покупки в серебре с последовательным ростом по уровням ветки; машины включены в доступное дерево техники.
- После изменения экономики пересозданы баланс-отчёт и русскоязычное представление данных.

Проверено:

- `node tools/generate-balance-report.mjs` — отчёт успешно пересоздан; изменение цен не добавило боевых выбросов.
- `node tools/generate-data-md.mjs` — документация данных успешно пересоздана, новые цены отображаются в таблицах веток.

Файлы, изменённые в версии `1.1.1.0v7`:

- `README.md`
- `data.csv`
- `BALANCE_REPORT.md`
- `data.md`

## Изменения 1.1.1.0v6

Изменено:

- Добавлены отсутствовавшие боевые характеристики танков `Chi-Ha`, `Chi-He` и `Mitsu 108` из ветки `Type 5 Heavy`: урон и пробитие трёх типов снарядов, перезарядка, броня, параметры орудия, а для `Mitsu 108` также HP и подвижность.
- Пересозданы баланс-отчёт и русскоязычное представление данных после изменения `data.csv`.

Проверено:

- `node tools/generate-balance-report.mjs` — отчёт успешно пересоздан; изменившиеся выбросы проверены вручную.
- `node tools/generate-data-md.mjs` — документация данных успешно пересоздана.

Файлы, изменённые в версии `1.1.1.0v6`:

- `README.md`
- `data.csv`
- `BALANCE_REPORT.md`
- `data.md`

## Изменения 1.1.1.0v5

Добавлено:

- `data.md` — упрощённое представление `data.csv` с понятными русскими названиями столбцов A–AG.
- Данные разделены на быстрый общий список, таблицы боевых характеристик по уровням и таблицы веток, экономики и доступности.
- Добавлен генератор `tools/generate-data-md.mjs`.
- В `AGENTS.md` добавлено обязательное обновление `data.md` после изменения `data.csv`.

Проверено:

- `node --check tools/generate-data-md.mjs`.
- `node tools/generate-data-md.mjs`.
- Количество строк техники в `data.md` сопоставлено с корректными строками `data.csv`.

Файлы, изменённые в версии `1.1.1.0v5`:

- `README.md`
- `AGENTS.md`
- `data.md`
- `tools/generate-data-md.mjs`

## Изменения 1.1.1.0v4

Изменено:

- Файл правил `codex.md` переименован в стандартный `AGENTS.md`.
- Инструкции из `AGENTS.md` теперь автоматически применяются при работе Codex в корне проекта и его вложенных каталогах.

Проверено:

- `AGENTS.md` находится в корне проекта.
- Содержимое правил сохранено после переименования.

Файлы, изменённые в версии `1.1.1.0v4`:

- `README.md`
- `AGENTS.md`
- удалён `codex.md`

## Изменения 1.1.1.0v3

Добавлено:

- `BALANCE_REPORT.md` с аудитом фактического влияния всех колонок `data.csv`.
- Для каждого доступного танка рассчитаны HP, эффективный и номинальный DPM, залп, пробитие, броня, скорость и итоговый вердикт: `ИМБА`, `Сильный`, `В балансе`, `Слабый` или `В ГОВНЕ`.
- Добавлены рекомендуемые коридоры характеристик по классам и уровням, списки лучших/худших машин и поиск разрывов стилистики веток.
- Будущие танки `AC=0` вынесены отдельно и не влияют на целевые медианы.
- Добавлен воспроизводимый генератор `tools/generate-balance-report.mjs`.
- Добавлен `codex.md` с обязательным требованием фиксировать каждое изменение проекта в `README.md`.

Проверено:

- `node --check tools/generate-balance-report.mjs`.
- `node tools/generate-balance-report.mjs`: отчёт построен для 423 танков, из них 192 участвуют в текущем подборе и 231 имеет флаг будущей техники `AC=0`.
- Формулы генератора сопоставлены с парсером `js/01-data.js` и боевой логикой `js/06-battle-logic.js`.

Файлы, изменённые в версии `1.1.1.0v3`:

- `README.md`
- `BALANCE_REPORT.md`
- `codex.md`
- `tools/generate-balance-report.mjs`

## Изменения 1.1.0.3v13

Добавлено:

- данные доступны только разработчикам

Проверено:

- данные доступны только разработчикам

Файлы, изменённые в версии `1.1.0.3v13`:

- данные доступны только разработчикам

## Изменения 1.1.0.3v12

Изменено:

- Расчёт вместимости боекомплекта сохранил прежнюю формулу `420 / время перезарядки`.
- Если формула даёт меньше `30` снарядов, вместимость БК теперь устанавливается в `30`.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.3v12`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.3v12`:

- `README.md`
- `index.html`
- `js/01-data.js`

## Изменения 1.1.0.3v11

Исправленно:

- Проведены работы ветке средних танков Германии `Е50`
- `Е50` - Х уровень, загружены характеристики, доступен к прокачке, текстуры отсутствуют
- `Panther II` - IX уровень, загружены характеристики, доступен к прокачке, текстуры отсутствуют
- `Panther F` - VIII уровень, загружены характеристики, доступен к прокачке, текстуры отсутствуют
- `Panther I` - VII уровень, загружены характеристики, доступен к прокачке, текстуры отсутствуют
- `VK 30.02 M` - VI уровень, загружены характеристики, доступен к прокачке, текстуры отсутствуют
- `VK 30.01 H` - V уровень, загружены характеристики, доступен к прокачке, текстуры отсутствуют
- `Pz. III J` - IV уровень, загружены характеристики, доступен к прокачке, текстуры отсутствуют
- Проведены работы по ветке противотанковых самоходных артилерийских установок Германии `WT auf E100`
- `WT auf E100` - X уровень, загружены характеристики, доступен к прокачке, текстуры отсутствуют
- `WT auf Pz.IV` - IX уровень, загружены характеристики, доступен к прокачке, текстуры отсутствуют
- `Rhm.-B WT` - VIII уровень, загружены характеристики, доступен к прокачке, текстуры отсутствуют
- `Sturer Emil` - VII уровень, загружены характеристики, доступен к прокачке, текстуры отсутствуют
- `Nashorn` - VI уровень, загружены характеристики, доступен к прокачке, текстуры отстутствуют
- Проведены работы по ветке самоходных артилерийских установок ГЕрмании `G.W. E100`
- `WT auf E100` - X уровень, загружены характеристики, доступен к прокачке, текстуры отсутствуют
- `G.W. Tiger` - IX уровень, загружены характеристики, доступен к прокачке, текстуры отсутствуют
- `G.W. Tiger (P)` - VIII уровень, загружены характеристики, доступен к прокачке, текстуры отсутствуют
- `G.W. Panther` - VII уровень, загружены характеристики, доступен к прокачке, текстуры отсутствуют
- `Hummel` - VI уровень, загружены характеристики, доступен к прокачке, текстуры отсутствуют
- `Grile` - V уровень, загружены характеристики, доступен к прокачке, текстуры отсутствуют
- `Pz.Sfl. Ivb` - IV уровень, загружены характеристики, доступен к прокачке, текстуры отсутствуют
- `Sturmpanzer I bison` - III уровень, загружены характеристики, доступен к прокачке, текстуры отсутствуют
- `G.Pz. Mk. VI (e)` - II уровень, загружены характеристики, доступен к прокачке, текстуры отсутствуют

Проверено:

- Документационное изменение, запуск `node --check` не требуется.

Файлы, изменённые в версии `1.1.0.3v11`:

- `README.md`
- `data.csv`

## Изменения 1.1.0.3v10

Исправлено:

- Парсер `data.csv` обновлён под новую схему колонок.
- Пробитие теперь читается по номеру снаряда: первый снаряд из `T`, второй из `U`, третий из `V`.
- Характеристики после пробития читаются из новых колонок: `W` броня, `X` шанс пробития, `Y` тип орудия, `Z` снарядов за выстрел, `AA` снарядов в барабане, `AB` разброс, `AC` вид/доступность танка, `AD` доп. способность.
- Боевая логика больше не ищет пробитие по типу снаряда, чтобы одинаковые типы в разных слотах могли иметь разное пробитие.
- Документация тестирования обновлена под новую карту колонок `data.csv`.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.3v10`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.3v10`:

- `README.md`
- `TESTING.md`
- `index.html`
- `js/01-data.js`
- `js/06-battle-logic.js`

## Изменения 1.1.0.3v9

Изменено:

- Стала доступна вся ветка тяжёлых танков Германии до `Maus`.
- У всей ветки `Maus` появились боевые характеристики в `data.csv`.
- `Pz. IV H` - V уровень, стартовый танк ветки Maus после `Pz IV D`.
- `VK 30.01 P` - VI уровень, продолжение ветки после `Pz. IV H`.
- `Tiger (P)` - VII уровень, развилка ветки после `VK 30.01 P`.
- `VK 100.01 P` - VIII уровень, прямой путь к Maus после `Tiger (P)`.
- `Mauschen` - IX уровень, предтоповый тяжёлый танк ветки.
- `Maus` - X уровень, финальный танк ветки.
- Текстуры сейчас есть только у самого `Maus`; у `Pz. IV H`, `VK 30.01 P`, `Tiger (P)`, `VK 100.01 P` и `Mauschen` текстур пока нет.

Проверено:

- Документационное изменение, запуск `node --check` не требуется.

Файлы, изменённые в версии `1.1.0.3v9`:

- `README.md`
- `data.csv`

## Изменения 1.1.0.3v8

Добавлено:

- Первый обычный бой каждого дня теперь даёт x2 опыта.
- Бонус x2 не тратится в тест-драйве и тренировке.
- На послебоевом экране добавлена строка бонуса `Первый бой дня x2`.
- В настройках добавлены кнопки экспорта и импорта сохранения через JSON-файл.
- Экспорт включает профиль, ресурсы, настройки, статистику, прогресс событий, ежедневных систем, боевого пропуска, контрактов, реплеи, опыт/состояние/экипаж/БК танков и отметки ежедневного x2.
- Импорт заменяет текущее сохранение и перезагружает игру.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.3v8`.

Не выполнено:

- `node --check` для всех JS-файлов: запуск в sandbox завершился ошибкой `CreateProcessAsUserW failed: 5`, запуск вне sandbox был отклонён.

Файлы, изменённые в версии `1.1.0.3v8`:

- `README.md`
- `index.html`
- `css/styles.css`
- `js/04-ui-developer.js`
- `js/06-battle-logic.js`

## Изменения 1.1.0.3v7

Добавлено:

- Все игроки, вошедшие в игру 4 июня 2026 года, получают одноразовый подарок `500` золота вчесть дня анонимного танкиста
- Получение подарка сохраняется в cookie/localStorage, чтобы награда не выдавалась повторно.
- После выдачи подарка показывается уведомление в ангаре.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.3v7`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.3v7`:

- `README.md`
- `index.html`
- `js/01-data.js`
- `js/08-startup.js`

## Изменения 1.1.0.3v6

Добавлено:

- После обычного боя потраченные снаряды автоматически докупаются до раскладки, с которой танк вышел в бой.
- Стоимость пополнения БК вычитается из серебра за бой и может сделать итоговую прибыль отрицательной.
- На послебоевом экране добавлена отдельная строка стоимости пополнения БК.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.3v6`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.3v6`:

- `README.md`
- `index.html`
- `js/06-battle-logic.js`

## Изменения 1.1.0.3v5

Исправлено:

- Боты теперь стреляют первым снарядом танка, а не снарядом с максимальным уроном.
- Проверка готовности выстрела у ПТ-ботов также использует первый снаряд.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.3v5`.

Не выполнено:

- `node --check` для всех JS-файлов: запуск проверки отклонён после сбоя sandbox `spawn setup refresh`.

Файлы, изменённые в версии `1.1.0.3v5`:

- `README.md`
- `index.html`
- `js/06-battle-logic.js`

## Изменения 1.1.0.3v4

Изменено:

- Кнопки поворота башни в мобильном управлении перенесены на правую сторону над кнопкой `Огонь`.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.3v4`.

Файлы, изменённые в версии `1.1.0.3v4`:

- `README.md`
- `index.html`
- `css/styles.css`

## Изменения 1.1.0.3v3

Исправлено:

- Мобильное управление теперь поддерживает одновременное удержание нескольких кнопок.
- Для виртуальных клавиш добавлены счётчики активных нажатий, чтобы отпускание одной кнопки не сбрасывало другие удерживаемые действия.
- Для панели мобильного управления отключены touch-жесты браузера, выделение и подсветка тапов.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.3v3`.

Проверено:

- `node --check js/08-startup.js`

Файлы, изменённые в версии `1.1.0.3v3`:

- `README.md`
- `index.html`
- `css/styles.css`
- `js/08-startup.js`

## Изменения 1.1.0.3v2

Изменено:

- В мобильном управлении касание по полю боя больше не двигает пушку и не стреляет.
- Поворот башни и выстрел в мобильном управлении теперь выполняются только отдельными кнопками.
- Для арты в мобильном управлении корпус больше не доворачивается к точке касания.
- Кнопки мобильного управления уменьшены, чтобы меньше закрывать экран боя.
- Поле зрения в мобильном управлении увеличено за счёт масштаба камеры `0.72`.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.3v2`.

Проверено:

- `node --check js/06-battle-logic.js`
- `node --check js/08-startup.js`

Файлы, изменённые в версии `1.1.0.3v2`:

- `README.md`
- `index.html`
- `css/styles.css`
- `js/06-battle-logic.js`
- `js/08-startup.js`

## Изменения 1.1.0.3v1

Добавлено:

- Добавлен ивент `Честь Дня России` с 1 по 15 июня.
- Для получения награды нужно сыграть 163 боя в обычных боевых режимах: ротный, взводный, дуэль, охота на командира, война или выживание.
- Награда ивента - бесплатный танк `Т-64`.
- Экран событий снова показывает сезонную ивент-панель с прогрессом и кнопкой получения награды.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.3v1`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.3v1`:

- `README.md`
- `index.html`
- `js/00-state.js`
- `js/03-events-store-profile.js`
- `mobile/index.html`
- `mobile/js/00-state.js`
- `mobile/js/03-events-store-profile.js`

## Изменения 1.1.0.2v29

Удалено:

- Ивент к 9 Мая больше не показывается на экране событий.
- Прогресс ивента к 9 Мая больше не начисляется после боёв.
- Экран событий теперь содержит ежедневную награду, ежедневные задания, боевой пропуск и контракты без старой майской панели.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.2v29`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.2v29`:

- `README.md`
- `index.html`
- `js/03-events-store-profile.js`

## Изменения 1.1.0.2v28

Изменено:

- Количество дней в месячном боевом пропуске теперь зависит от месяца.
- Февраль всегда создаёт 28 дней, месяцы апрель/июнь/сентябрь/ноябрь - 30 дней, остальные - 31 день.
- Текст прогресса боевого пропуска теперь показывает фактическое количество дней месяца.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.2v28`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.2v28`:

- `README.md`
- `index.html`
- `js/03-events-store-profile.js`

## Изменения 1.1.0.2v27

Исправлено:

- Выход из незавершённого обычного боя теперь засчитывает бой как поражение.
- Ежедневные задания, боевой пропуск, контракты, статистика и награды получают прогресс сразу при выходе через `Назад`.
- Тест-драйв и тренировка по-прежнему не засчитывают прогресс.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.2v27`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.2v27`:

- `README.md`
- `index.html`
- `js/08-startup.js`

## Изменения 1.1.0.2v26

Изменено:

- Использование ремкомплекта игроком теперь стоит `200` серебра.
- Использование огнетушителя игроком теперь стоит `500` серебра.
- Кнопки ремкомплекта и огнетушителя в бою отключаются, если серебра не хватает.
- После платного применения расходника верхняя панель ресурсов обновляется.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.2v26`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.2v26`:

- `README.md`
- `index.html`
- `js/00-state.js`
- `js/06-battle-logic.js`

## Изменения 1.1.0.2v25

Добавлено:

- Снаряды стали платными и хранятся отдельным боекомплектом для каждого танка.
- Цена первого и третьего снаряда равна `10 * уровень^2` серебра, второго - `50 * уровень^2` серебра.
- Вместимость боекомплекта равна `round(420 / перезарядка)`, чтобы хватало примерно на 7 минут стрельбы по КД.
- В ангаре добавлена кнопка `Боекомплект`, где видно количество снарядов разных типов и можно изменить раскладку.
- В бою на панели снарядов показывается остаток БК, пустые типы отключаются, а в обычных боях снаряды списываются после выстрела.
- Тест-драйв и тренировка не тратят сохранённые снаряды.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.2v25`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.2v25`:

- `README.md`
- `index.html`
- `css/styles.css`
- `js/01-data.js`
- `js/02-hangar-tech-tree.js`
- `js/04-ui-developer.js`
- `js/06-battle-logic.js`
- `js/08-startup.js`

## Изменения 1.1.0.2v24

Изменено:

- Код уникальной особенности `Плавающий` изменён с `00` на `10`, чтобы таблица не удаляла ведущий ноль.
- Водный штраф теперь отключается у танков с кодом особенности `10`.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.2v24`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.2v24`:

- `README.md`
- `index.html`
- `js/01-data.js`
- `js/06-battle-logic.js`

## Изменения 1.1.0.2v23

Добавлено:

- В `data.csv` добавлен столбец `AE` для уникальных особенностей танков.
- Особенности записываются подряд двухзначными кодами: например `101112` читается как `10`, `11`, `12`.
- Реализован код `10`: плавающий танк не получает штраф скорости при движении в воде.
- Для обычных танков добавлен штраф скорости в воде.
- Уникальные особенности показываются в ангаре и сравнении танков.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.2v23`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.2v23`:

- `README.md`
- `index.html`
- `js/01-data.js`
- `js/02-hangar-tech-tree.js`
- `js/06-battle-logic.js`

## Изменения 1.1.0.2v22

Добавлено:

- В `data.csv` добавлен новый тип техники по флагу `3`: developer-only танки.
- Танки с флагом `3` скрыты в обычном режиме и появляются только после включения developer mode через консоль.
- Developer-only танки не попадают в магазин, контейнеры, боевой пропуск и случайный подбор ботов.
- При включении developer mode ангар и фильтры сразу перерисовываются, а в фильтре типа появляется пункт `Разработчик`.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.2v22`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.2v22`:

- `README.md`
- `index.html`
- `js/01-data.js`
- `js/02-hangar-tech-tree.js`
- `js/03-events-store-profile.js`
- `js/04-ui-developer.js`
- `js/08-startup.js`

## Изменения 1.1.0.2v21

Добавлено:

- Если игрок или бот пытается ехать и не смещается 5 секунд при исправной ходовой, танк переносится в ближайшую свободную точку, откуда можно продолжить движение.
- Боты теперь получают боевые расходники и автоматически используют ремкомплект при повреждённых модулях.
- Боты автоматически используют огнетушитель при пожаре.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.2v21`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.2v21`:

- `README.md`
- `index.html`
- `js/06-battle-logic.js`

## Изменения 1.1.0.2v20

Исправлено:

- Сломанная ходовая теперь блокирует не только движение, но и поворот корпуса танка.
- Для ботов сломанная ходовая также блокирует разворот корпуса при движении и наведении без башни.
- У классов `КТ` и `БТР` модуль ходовой в HUD и реплеях называется `Колёса` вместо `Гусеницы`.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.2v20`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.2v20`:

- `README.md`
- `index.html`
- `js/06-battle-logic.js`

## Изменения 1.1.0.2v19

Добавлено:

- В боевой HUD добавлена панель прогресса матча.
- Панель показывает счет живых союзников и врагов, текущую цель режима и прогресс захвата базы/точек.
- Для режима войны показывается прогресс точек и активный захват точки или базы.
- Для выживания показывается число оставшихся участников и уровень усиления.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.2v19`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.2v19`:

- `README.md`
- `index.html`
- `js/07-battle-render.js`

## Изменения 1.1.0.2v18

Исправлено:

- Если изображение не загрузилось, браузерная иконка битой картинки больше не показывается.
- Для всех `img` добавлена общая обработка ошибок загрузки: битая картинка скрывается, а при успешной последующей загрузке снова показывается.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.2v18`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.2v18`:

- `README.md`
- `index.html`
- `css/styles.css`
- `js/08-startup.js`

## Изменения 1.1.0.2v17

Добавлено:

- В профиль добавлен раздел реплеев с последними 5 боями.
- После боя сохраняется сводка: дата, результат, режим, карта, танк, урон, попадания, выстрелы и награды.
- В реплее сохраняется журнал выстрелов: кто стрелял, по кому, тип снаряда, координаты выстрела и попадания/промаха, пробил/не пробил, урон, блокированный урон и повреждённый модуль.
- Журнал одного боя ограничен 160 выстрелами, чтобы не раздувать localStorage/cookie.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.2v17`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.2v17`:

- `README.md`
- `index.html`
- `js/03-events-store-profile.js`
- `js/06-battle-logic.js`
- `js/08-startup.js`

## Изменения 1.1.0.2v16

Изменено:

- Ремкомплект больше не одноразовый: после применения уходит на перезарядку 30 секунд.
- Огнетушитель остаётся одноразовым расходником на бой.
- Панель боя и HUD показывают оставшееся время перезарядки ремкомплекта.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.2v16`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.2v16`:

- `README.md`
- `index.html`
- `js/06-battle-logic.js`
- `js/07-battle-render.js`

## Изменения 1.1.0.2v15

Добавлено:

- В бою у танков появились модули: топливный бак, БК башни, БК корпуса, гусеницы, орудие и башня.
- Повреждённый БК замедляет перезарядку, сломанное орудие блокирует стрельбу, сломанные гусеницы блокируют движение, сломанная башня блокирует её поворот.
- Топливный бак может вызвать пожар при повреждении; пожар наносит периодический урон до затухания или применения огнетушителя.
- Добавлены боевые расходники игрока: ремка на клавише `4` ремонтирует повреждённые модули, огнетушитель на клавише `5` тушит пожар.
- Переключатель залпа/барабана у многоствольных орудий перенесён с клавиши `4` на клавишу `6`.
- В боевой HUD добавлена панель состояния модулей, ремки, огнетушителя и пожара.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.2v15`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.2v15`:

- `README.md`
- `index.html`
- `js/06-battle-logic.js`
- `js/07-battle-render.js`
- `js/08-startup.js`

## Изменения 1.1.0.2v14

Исправлено:

- Коллизия камней в бою подогнана под видимую текстуру завала, а не под прежний завышенный прямоугольник.
- Видимые остовы танков на картах теперь имеют коллизию для движения, маршрутов ботов и попаданий снарядов.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.2v14`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.2v14`:

- `README.md`
- `index.html`
- `js/06-battle-logic.js`

## Изменения 1.1.0.2v13

Добавлено:

- `./img/logo.png` подключён в `index.html` как favicon.
- `./img/logo.png` подключён как `apple-touch-icon` для ярлыков на мобильных устройствах.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.2v13`.

Файлы, изменённые в версии `1.1.0.2v13`:

- `README.md`
- `index.html`

## Изменения 1.1.0.2v12

Изменено:

- Боевой пропуск теперь продвигается по первому незабранному доступному дню, а не по текущему календарному дню месяца.
- После получения награды за день следующий день боевого пропуска становится доступен только на следующий календарный день.
- Финальная награда боевого пропуска теперь открывает не всю нацию, а выбранную ветку до прокачиваемого танка X уровня.
- В выборе финальной награды показываются все прокачиваемые танки X уровня и список техники, которая будет открыта в выбранной ветке.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.2v12`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.2v12`:

- `README.md`
- `index.html`
- `js/03-events-store-profile.js`

## Изменения 1.1.0.2v11

Исправлено:

- Отключено выделение текста в интерфейсе игры через CSS `user-select: none`.
- Отключено перетаскивание изображений и canvas через CSS и обработчик `dragstart`.
- Добавлен обработчик `selectstart`, который блокирует выделение текста на странице.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.2v11`.

Проверено:

- `node --check` для изменённого JS-файла.

Файлы, изменённые в версии `1.1.0.2v11`:

- `README.md`
- `index.html`
- `css/styles.css`
- `js/08-startup.js`

## Изменения 1.1.0.2v10

Исправлено:

- Для включения developer mode снова обязателен секретный ключ: `dev("ключ")`.
- Резервный API `tw` сохранён, но тоже требует ключ: `tw.dev("ключ")`.
- Устойчивая установка консольных команд из версии `1.1.0.2v9` сохранена.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.2v10`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.2v10`:

- `README.md`
- `index.html`
- `js/04-ui-developer.js`

## Изменения 1.1.0.2v9

Исправлено:

- Режим разработчика теперь устанавливает консольные команды по одной, чтобы конфликт одного имени не ломал весь набор команд.
- В версии `1.1.0.2v9` временно был разрешён `dev()` без ключа; в `1.1.0.2v10` обязательный ключ возвращён.
- Добавлен резервный объект `tw` / `tanksWarsDev` для консоли. Начиная с `1.1.0.2v10`, включение выполняется через `tw.dev("ключ")`.
- Добавлена команда `devHelp()` для вывода подсказки по developer mode.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.2v9`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.2v9`:

- `README.md`
- `index.html`
- `js/04-ui-developer.js`

## Изменения 1.1.0.2v8

Добавлено:

- У ботов появились характеры: `Тактик`, `Штурмовик`, `Осторожный`, `Снайпер`, `Разведчик`, `Охотник`.
- Характер влияет на выбор цели, дистанцию боя, любовь к укрытиям, агрессию, отступление при низком HP, скорость принятия решений и требования к наведению перед выстрелом.
- Характер бота теперь показывается в списке команд рядом с названием танка.
- Cache-busting в `index.html` обновлён до `?v=1.1.0.2v8`.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.2v8`:

- `README.md`
- `index.html`
- `js/06-battle-logic.js`
- `js/07-battle-render.js`

## Изменения 1.1.0.2v7

Исправлено:

- Добавлены версионные query-параметры `?v=1.1.0.2v7` к `css/styles.css` и всем JS-файлам в `index.html`.
- Это заставляет браузер и хостинг/CDN загружать свежий код после деплоя, а не использовать старые закэшированные файлы.

Файлы, изменённые в версии `1.1.0.2v7`:

- `README.md`
- `index.html`

## Изменения 1.1.0.2v6

Исправлено и доделано:

- Тест-драйв теперь держит бой в режиме `Дуэль` до выхода в ангар, а не возвращает глобальный режим сразу после старта.
- Тест-драйв больше не засчитывает прогресс ивента после боя.
- Добавлено видимое описание роли класса танка в ангаре.
- Подробный послебоевой экран теперь показывает роль класса и задачу роли.

Проверено:

- `node --check` для всех JS-файлов.

Файлы, изменённые в версии `1.1.0.2v6`:

- `README.md`
- `js/00-state.js`
- `js/02-hangar-tech-tree.js`
- `js/06-battle-logic.js`
- `js/08-startup.js`

## Изменения 1.1.0.2v5

Исправлено:

- Новая кнопка `Назад` теперь показывается не только для экранов, открытых через боковое меню, но и для экранов `Сравнить` и `Экипаж`.
- Причина бага: `Сравнить` и `Экипаж` открывали overlay напрямую и не проходили через общий `openOverlay()`, где включалась кнопка.

Файлы, изменённые в версии `1.1.0.2v5`:

- `README.md`
- `js/02-hangar-tech-tree.js`

## Изменения 1.1.0.2v4

Исправлено:

- Кнопка `Назад` полностью переделана с нуля.
- Старая кнопка `#backButton` внутри `#screenOverlay` скрыта и больше не используется.
- Новая кнопка `.overlayBackButton` создаётся через JavaScript, добавляется напрямую в `#game`, показывается при открытии overlay и скрывается при закрытии.
- Закрытие overlay теперь выполняется на `pointerdown` собственной кнопки, без зависимости от слоёв `overlayContent` и без ожидания события `click`.

Файлы, изменённые в версии `1.1.0.2v4`:

- `README.md`
- `css/styles.css`
- `js/04-ui-developer.js`

## Изменения 1.1.0.2v3

Исправлено:

- Кнопка `Назад` поднята над всеми overlay-слоями через высокий `z-index` и явный `pointer-events: auto`.
- Закрытие overlay теперь ловится на уровне `document` в capture-фазе для `pointerdown`, `pointerup`, `mousedown`, `mouseup`, `click` и `touchstart`.
- Это исправляет ситуацию, когда обычный обработчик кнопки не срабатывал из-за перехвата события другим слоем или обработчиком.

Файлы, изменённые в версии `1.1.0.2v3`:

- `README.md`
- `css/styles.css`
- `js/04-ui-developer.js`

## Изменения 1.1.0.2v2

Исправлено:

- Кнопка `Назад` в overlay теперь принудительно останавливает всплытие события и закрывает текущий экран.
- Глобальная блокировка `mousedown` больше не применяется ко всему документу. Она работает только во время боя и только на `battleCanvas`.
- Из-за этого снова должны открываться выпадающие списки в сравнении танков и фильтрах.
- В сравнении танков список выбора расширен с танков в ангаре до всех загруженных не будущих танков.

Файлы, изменённые в версии `1.1.0.2v2`:

- `README.md`
- `js/02-hangar-tech-tree.js`
- `js/04-ui-developer.js`
- `js/08-startup.js`

## Изменения 1.1.0.2v1

Добавлено и исправлено:

- Ежедневная награда теперь может выдавать технику из контейнерного пула.
- Если ежедневная награда выдала танк, который уже есть у игрока, начисляется компенсация золотом.
- Ежедневные задания уже поддерживают условия по конкретному танку, нации и классу техники.
- Сравнение танков расширено: добавлены нация, класс, все снаряды, урон, пробитие, тип орудия, магазин, разброс, шанс пробития, стоимость и флаги доступности.
- В сравнении зелёным подсвечивается лучшая характеристика, красным худшая. Для задержек и разброса меньшее значение считается лучше.
- Экипаж больше нельзя бесплатно мгновенно прокачать кнопкой. Он качается боевым опытом экипажа, который равен 50% обычного опыта.
- Улучшение члена экипажа за золото оставляет бонус `+5%` к характеристикам этого члена.
- Убраны ссылки на отсутствующие изображения `img/kont.png` и `img/rezimi/*.png`.

Файлы, изменённые в версии `1.1.0.2v1`:

- `README.md`
- `css/styles.css`
- `js/00-state.js`
- `js/02-hangar-tech-tree.js`
- `js/03-events-store-profile.js`
- `js/04-ui-developer.js`

## Проверка версии

Минимальная проверка после правок:

```powershell
node --check js/00-state.js
node --check js/01-data.js
node --check js/02-hangar-tech-tree.js
node --check js/03-events-store-profile.js
node --check js/04-ui-developer.js
node --check js/05-battle-maps.js
node --check js/06-battle-logic.js
node --check js/07-battle-render.js
node --check js/08-startup.js
```
