# Tanks Wars APK

Android WebView wrapper for the local Tanks Wars web game.

The packaged web files are stored in `app/src/main/assets/www`.
The resulting APK is copied to `output/TanksWars-debug.apk`.
